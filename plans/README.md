# Implementation Plans

Generated by the improve skill on 2026-06-12 (quick audit at commit `70aa8ac`,
v0.71.0). Execute in the order below. Each executor: read the plan fully
before starting, honor its STOP conditions, and update your row when done.

> Note: the dated `YYYY-MM-DD-*.md` files in this folder are planning docs
> from other workflows (audits, design reviews) — they are not executor plans
> and are not tracked in this index. Numbered `NNN-*.md` files are.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | CSV quote-aware record splitting + coverage gate | P1 | M | — | DONE |
| 002 | Sentry PHI scrub hardening | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- None. 001 and 002 touch disjoint files and can run in parallel, but if run
  sequentially do 001 first (P1, and it owns `vitest.config.ts`).
- Both plans edit this README's status column — if run in parallel worktrees,
  expect a trivial merge in this file only.

## Findings considered and rejected (2026-06-12 quick audit)

- **Hardcoded PIN in `PasswordGate.tsx:6`** — by design; documented in-component
  and in README as a shoulder-surfing control only, not a security boundary.
- **Duplicate `'Glycopyrrolate'` entry in `DRUG_TIME_MATCHERS`
  (`csvUtils.ts:140`)** — dead but harmless; no glyco header exists in the
  real REDCap export. Do not "fix" without a fixture proving the intended
  alternate spelling.
- **Sentry session replay on a clinical app** — `maskAllText` +
  `blockAllMedia` already set; correctly configured.
- **`tracesSampleRate: 1.0`** — cost, not correctness; tune independently if
  Sentry quota becomes an issue.

## Prior-audit reconciliation (2026-06-10 audit, v0.65.0)

All four outstanding items (OI-0 gitignore, OI-1 App.tsx decomposition,
OI-2 Zod save-path, OI-3 TestingLogForm split) shipped in v0.66–v0.68 and are
closed. See `2026-06-10-outstanding-audit-items.md` for history.
